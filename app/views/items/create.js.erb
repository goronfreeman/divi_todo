$("#list-<%= @list.id %> > .list-items").append("<%= j render partial: 'items/item', locals: {item: @item, list: @list} %>");

$('.item').last().hide().show('normal');

$('#title, #description').val('');
